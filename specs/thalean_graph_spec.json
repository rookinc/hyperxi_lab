{
  "spec_version": "1.1.0",
  "schema": "cori.graph.spec.v1",

  "name": "Thalean Graph",
  "graph_id": "AT4val[60,6]",

  "provenance": {
    "construction": "dodecahedral_flag_transport",
    "paper_title": "A Dodecahedral Transport Construction of the Arc-Transitive Graph AT4val[60,6]",
    "doi": "10.5281/zenodo.19043356"
  },

  "graph": {
    "vertices": 60,
    "edges": [
      /* ============================================================
         FULL EDGE LIST REQUIRED HERE
         Replace with your real 120 edges from G60
         Format: [u, v] with 0 <= u,v < 60
         ============================================================ */

      [0, 1],
      [0, 7],
      [0, 13]

      /* ... CONTINUE UNTIL ALL 120 EDGES ARE INCLUDED ... */
    ]
  },

  "quotients": {
    "G60_to_G30": {
      "type": "involution_pairing",
      "map": [
        /* length 60 → values in [0..29] */
        /* paste your real quotient map here */
      ]
    },
    "G30_to_G15": {
      "type": "involution_pairing",
      "map": [
        /* length 30 → values in [0..14] */
      ]
    }
  },

  "signed_lift": {
    "base_graph": "G15",
    "type": "2-lift",

    "edge_order": [
      /* ordered list of G15 edges */
      /* e.g. [ [0,1], [1,2], ... ] */
    ],

    "signs": [
      /* +1 / -1 list aligned with edge_order */
      /* length = number of edges in G15 (30) */
    ],

    "counts": {
      "positive": 10,
      "negative": 20
    }
  },

  "sector_system": {
    "description": "edge-sector incidence structure",

    "matrix_shape": [15, 30],

    "incidence_matrix": [
      /* 15 x 30 matrix (rows = sectors, cols = edges) */
      /* values typically 0/1 or signed */
    ]
  },

  "transport": {
    "generators": ["F", "V", "S"],

    "state_space": {
      "type": "chamber",
      "cardinality": 60
    },

    "rules": {
      "description": "local transport on dodecahedral flags"
    }
  },

  "render": {
    "default_layout": "circular",
    "ordering": "vertex_index",
    "style": {
      "node_size": 12,
      "edge_width": 0.5
    }
  },

  "invariants": {
    "degree": 4,
    "edges": 120,
    "connected": true,
    "diameter": 6
  }
}
